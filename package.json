{
  "name": "gym-manager-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/app.tsx --outdir dist --target browser --sourcemap=linked && cp src/index.html src/styles.css src/boot.js dist/",
    "check": "tsc --noEmit",
    "smoke": "bun run build && python3 scripts/smoke.py"
  },
  "dependencies": {
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
